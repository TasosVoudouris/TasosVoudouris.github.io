# Polynomial splitting and roots of unity

Run:

```bash
python polynomial_splitting.py
```

Small F_433 example showing Horner evaluation and the identity `A(x)=B(x^2)+xC(x^2)` behind radix-2 FFT recursion.
