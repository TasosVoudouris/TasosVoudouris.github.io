# Packed secret sharing

Run:

```bash
python packed_secret_sharing.py
```

Demonstrates packing several secrets into one polynomial, erasure tolerance for the base degree, linear addition, and degree growth under multiplication.
