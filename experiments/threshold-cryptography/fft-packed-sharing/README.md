# FFT-based packed secret sharing

Run:

```bash
python fft_packed_sharing.py
```

Fixed toy parameters keep the finite-field radix-2/radix-3 example reproducible and fast. This is educational code, not production MPC software.
