# Robust secret sharing / Gao decoding

This folder contains the recovered `ReedGao.py` implementation used by the robust-secret-sharing article. It is preserved as research code; inspect parameters and dependencies before treating it as a reusable library.
