# SIS toy experiment

This is a deliberately tiny exhaustive-search demonstration of the condition

`A z = 0 (mod q)` with a short nonzero integer vector `z`.

It is **not** a secure SIS parameter set and is not meant to benchmark SIS hardness.
