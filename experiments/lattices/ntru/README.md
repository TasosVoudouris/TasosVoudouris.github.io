# NTRU toy experiment

A tiny implementation of the classic scaling convention

`h = p g f^{-1} (mod q)`, `e = r h + m`.

It is designed only to validate the algebra in the article. The parameters and sampling
are not suitable for cryptographic use.
