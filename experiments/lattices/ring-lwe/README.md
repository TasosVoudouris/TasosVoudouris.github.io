# Ring-LWE toy experiment

A small negacyclic ring `Z_q[x]/(x^N + 1)` implementation showing an RLWE-style
public key and bit-polynomial encryption.

This code is intentionally tiny and insecure. In particular, it is **not ML-KEM**.
